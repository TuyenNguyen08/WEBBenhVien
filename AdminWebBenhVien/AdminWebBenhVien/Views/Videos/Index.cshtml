@using AdminWebBenhVien.Controllers
@using AdminWebBenhVien.Models

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="wrapper">
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Danh sách tin tức<span style="color:orange"></span></h1>
            </div>
        </div>
        <br />
        <p>
            <a asp-action="Create"><button class="k-primary">Thêm</button></a>
        </p>
        <div class="row">
            <div class="col-lg-12">
                <kendo-grid name="gridVideo" height="550" resizable="true">
                    <datasource type="DataSourceTagHelperType.Ajax" server-operation="false" page-size="20">
                        <transport>
                            <read url="@Url.Action(nameof(VideosController.Video_Read))" type="get" />
                        </transport>
                        <schema>
                            <model id="@nameof(VideoIndexViewModel.Id)">
                                <fields>
                                    <field name="@nameof(VideoIndexViewModel.Id)" />
                                    <field name="@nameof(VideoIndexViewModel.TieuDe)" />
                                    <field name="@nameof(VideoIndexViewModel.GioiThieu)" />
                                    <field name="@nameof(VideoIndexViewModel.Xem)" type="number" />
                                    <field name="@nameof(VideoIndexViewModel.DuongDanFile)" />
                                    <field name="@nameof(VideoIndexViewModel.NgonNguId)" />
                                    <field name="@nameof(VideoIndexViewModel.NgonNgu)" />
                                    <field name="@nameof(VideoIndexViewModel.NgayTao)" type="date" />
                                    <field name="@nameof(VideoIndexViewModel.NguoiTao)" />
                                    <field name="@nameof(VideoIndexViewModel.NgaySua)" type="date" />
                                    <field name="@nameof(VideoIndexViewModel.NguoiSua)" />
                                </fields>
                            </model>
                        </schema>
                    </datasource>
                    <groupable enabled="true" />
                    <sortable enabled="true" />
                    <filterable enabled="true" />
                    <scrollable enabled="true" />
                    <pageable button-count="5" refresh="true" page-sizes="new int[] { 10, 20, 30, 40, 50, 100 }">
                    </pageable>
                    <filterable enabled="true" />
                    <columns>
                        <column title="Thao tác" width="100" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-align-center" }'>
                            <commands>
                                <column-command text="Sửa" name="btnCapNhat1" class-name="k-primary" click="btnCapNhat_Click"></column-command>
                            </commands>
                        </column>
                        <column field="@nameof(VideoIndexViewModel.Id)" title="ID" width="100" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-align-center" }' />
                        <column field="@nameof(VideoIndexViewModel.TieuDe)" title="Tiêu đề" width="300" />
                        <column field="@nameof(VideoIndexViewModel.GioiThieu)" title="Giới thiệu" width="300" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-ellipsis" }' />
                        <column field="@nameof(VideoIndexViewModel.Xem)" title="Lượt xem" width="100" format="{0:N0}" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-align-right" }' />
                        <column field="@nameof(VideoIndexViewModel.DuongDanFile)" title="Đường link" width="200" />
                        <column field="@nameof(VideoIndexViewModel.NgonNgu)" title="Ngôn ngữ" width="100" />
                        <column field="@nameof(VideoIndexViewModel.NgayTao)" title="Ngày tạo" width="150" format="{0:dd/MM/yy HH:mm:ss}" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-align-center" }' />
                        <column field="@nameof(VideoIndexViewModel.NguoiTao)" title="Người tạo" width="200" />
                        <column field="@nameof(VideoIndexViewModel.NgaySua)" title="Ngày sửa" width="150" format="{0:dd/MM/yy HH:mm:ss}" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-align-center" }' />
                        <column field="@nameof(VideoIndexViewModel.NguoiSua)" title="Người sửa" width="200" />
                        <column title="Thao tác" width="100" html-attributes='new Dictionary<string, object>{ ["class"] = "bv-column-align-center" }'>
                            <commands>
                                <column-command text="Sửa" name="btnCapNhat2" class-name="k-primary" click="btnCapNhat_Click"></column-command>
                            </commands>
                        </column>
                    </columns>
                </kendo-grid>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <script type="text/javascript">
        var urlCapNhat = "@Url.Action(nameof(VideosController.Edit), new { id = Constants.PARAM_VIDEO_ID })";
        var urlXoa = "@Url.Action(nameof(VideosController.Delete), new { id = Constants.PARAM_XOA_VIDEO_ID })";

        function btnCapNhat_Click(e) {

            e.preventDefault();

            var kendoGrid = $("#gridVideo").data("kendoGrid");
            var tr = $(e.currentTarget).closest("tr");
            var data = kendoGrid.dataItem(tr);

            var url = urlCapNhat.replace("@Constants.PARAM_VIDEO_ID", data.id);
            window.open(url, "_blank");
        };
         function btnXoa_Click(e) {
            e.preventDefault();

             var kendoGrid = $("#gridVideo").data("kendoGrid");
            var tr = $(e.currentTarget).closest("tr");
            var data = kendoGrid.dataItem(tr);
            var url = urlXoa.replace("@Constants.PARAM_XOA_VIDEO_ID", data.id);
            window.open(url, "_blank");

        };
    </script>
}
